version: '3'
services:

  #PHP Service
  app:
    container_name: mediusware-app
    build:
      context: .
      dockerfile: docker/Dockerfile-php7.4
    restart: unless-stopped
    tty: true
    environment:
      SERVICE_NAME: app
      SERVICE_TAGS: dev
    working_dir: /var/www/html
    volumes:
      - ./:/var/www/html
    extra_hosts:
      - "host.docker.internal:${HOST_IP}"
      - "www.mediusware.test:${HOST_IP}"
    networks:
      - mediusware

  #Nginx Service
  nginx:
    container_name: mediusware-nginx
    image: nginx:1.17
    restart: unless-stopped
    tty: true
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./:/var/www/html
      - ./docker/nginx.conf:/etc/nginx/conf.d/default.conf
    extra_hosts:
      - "host.docker.internal:${HOST_IP}"
      - "www.mediusware.test:${HOST_IP}"
    networks:
      - mediusware

  #MySQL Service
  database:
    container_name: mediusware-mysql
    image: mysql:5.7
    restart: unless-stopped
    tty: true
    ports:
      - "3306:3306"
    command: [
      "mysqld",
      "--default-authentication-plugin=mysql_native_password",
      "--character-set-server=utf8mb4",
      "--collation-server=utf8mb4_unicode_ci"
    ]
    environment:
      MYSQL_DATABASE: mediusware_db_local
      MYSQL_ROOT_PASSWORD: password
    volumes:
      - ./docker/database_data_dir:/var/lib/mysql
    extra_hosts:
      - "host.docker.internal:${HOST_IP}"
      - "www.mediusware.test:${HOST_IP}"
    networks:
      - mediusware

  adminer:
    container_name: mediusware-adminer
    image: adminer
    restart: always
    ports:
      - 1010:8080
    networks:
      - mediusware

#Docker Networks
networks:
  mediusware:
    driver: bridge